# Day 28 笔记 — 基础 RAG Pipeline（下）

## 四步流水线（一句话各自记一遍）
- ① retrieve：问题嵌入 → 和库逐条算余弦相似度 → top-k =
- ② 拼 context：top-k 编号塞进 prompt、锁死作答范围 =
- ③ 生成带引用：照资料答 + 标 [编号]，temperature=0 =
- ④ 防幻觉两道闸：检索分阈值 / prompt 限定未提及 =

## 思考题答案
1. 瓶颈在检索还是生成？先优化哪头：

2. context 越长越好吗（top-k 调到 10）：

3. 防幻觉阈值太高/太低分别会怎样：

4. 引用对不上原文，链路哪步出问题：

5. 模型可替换（只换 generate）对选型/降本意味着：

## 今日卡点 / 疑问
- nomic-embed-text 对中文短句区分度有限（实测库里直答片段也不一定排第一）→ 真项目里要做：

## 一句话总结
