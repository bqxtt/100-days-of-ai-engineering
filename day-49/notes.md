# Day 49 笔记 · Agent 安全性（注入防御 / 输出验证 / 权限控制）

## 思考题答案
1. 间接注入比直接注入难防在哪？我会在管线哪几个点做隔离：

2. 模型用合法工具 refund 给攻击者退款怎么办（最小权限/限额/HITL 各补哪层）：

3. 给代码分析 Agent 加 write_file，安全边界要加什么（对照四道防御）：

## 攻击面 ↔ 后端类比（各记一句）
- 直接注入  ↔ SQL 注入：
- 间接注入  ↔ 存储型 XSS：
- 越权工具  ↔ RBAC/IDOR：
- 数据外泄  ↔ 出参泄内部字段：

## 四道防御复盘
- 输入隔离（≈参数化查询）：
- 输出 schema 校验（≈出参白名单）：
- 工具最小权限（≈RBAC）：
- 人在回路 HITL（≈高危二次审批）：

## 实战观察（跑 uv run）
- ① 无防御这次中招了吗（多跑几次看随机性）：
- ② 隔离后密钥还泄露吗 / 还总结商品吗：
- ③ delete_db 被拒、refund 挂起→人确认才执行：

## 今日卡点 / 疑问

## 一句话总结
Prompt Injection = LLM 版 SQL 注入/XSS，病根是模型不分指令与数据；防御=隔离+输出白名单+最小权限+HITL，纵深叠加、与模型无关。
