# Day 27 笔记 — 基础 RAG Pipeline（上）：建索引侧

## 思考题答案
1. 为什么建库与查询必须同一嵌入模型？换模型后旧库还能用吗：

2. chunk 50字 vs 1000字 检索质量各怎样退化？我的文档该切多大：

3. numpy 暴力 `M @ q` 在千万级向量的瓶颈（→ 为什么要向量数据库）：

4. 索引离线写一次 / 问答在线读高频，和读写分离·缓存预热的相似：

## 今日卡点 / 疑问

## 一句话总结


---

## 索引侧四步速记（明天问答侧要接这里）
load 加载 → chunk 分块 → embed 嵌入(nomic-embed-text,768维) → index 入库(numpy 归一化矩阵)
- 铁律：建库 / 查询 同模型同维度
- 检索：query 嵌入后 `M @ q` 取 Top-k，归一化后点积=余弦相似度
- 今天产出：可被语义检索的向量库 + 一次验证检索 → Day28 拿命中片段拼 prompt 调模型
