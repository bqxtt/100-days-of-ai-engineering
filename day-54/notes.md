# Day 54 笔记 · Phase 4 生产化（容错 / 扩展 / 成本控制）

## 思考题答案
1. 重试用固定 1s vs 指数退避，对已过载的模型服务的区别（雪崩）：

2. 语义缓存要让「相似问题也命中」，键换成什么？引入什么新风险：

3. fallback 兜底体验是「降级」非「报错」，但会掩盖故障——监控补哪个指标：

## 三层护甲对照后端（各记一条）
- 容错 retry/timeout/fallback ←→ 熔断/重试/降级：
- 扩展 无状态/并发/队列 ←→ 无状态服务/负载均衡/Kafka 削峰：
- 成本 小模型路由/缓存/step 上限 ←→ 模型分级/Redis/限流：

## 实战观察（跑 uv run 看 🔁💾💰）
- 🔁 重试在第几次救回、退避秒数对不对：
- 💾 第3问命中缓存了吗，真调用降到几次：
- 💰 3 次提问真调几次、省几次：

## 今日卡点 / 疑问

## 一句话总结
生产化 = 假设下游必挂/流量必爆/预算必紧，给 Day42 核心循环包三层外围护甲，核心一行不动。
